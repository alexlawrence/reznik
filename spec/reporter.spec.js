/*var subject = require('../src/reporter.js');

describe('reporter', function() {

    describe('toJSON', function() {

        var evaulationResult = {
            modules: {
                'a': ['b', 'c', 'd']
            },
            errors: ['Error']
        };

        var result = subject.toJSON(evaulationResult);

        expect(result).toBe()
    });

});*/